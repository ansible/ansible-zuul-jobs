---
- hosts: all
  tasks:
    - name: Setup build-container-image role
      include_role:
        name: build-container-image

    - name: Pause the job
      zuul_return:
        data:
          zuul:
            pause: true
