---
- project-template:
    name: ansible-python-jobs
    description: |
      A common set of python jobs to run.
    check:
      jobs:
        - tox-linters:
            nodeset: fedora-latest-1vcpu
    gate:
      jobs:
        - tox-linters:
            nodeset: fedora-latest-1vcpu
