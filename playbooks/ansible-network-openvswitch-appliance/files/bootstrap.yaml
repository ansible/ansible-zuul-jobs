---
- hosts: openvswitch
  tasks:
    - name: Install openvswitch package
      become: true
      package:
        name: openvswitch
        state: present

    - name: Start openvswitch
      systemd:
        name: openvswitch
        state: started
      become: true
