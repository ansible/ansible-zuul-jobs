---
- hosts: openvswitch
  tasks:
    - name: Install openvswitch package
      become: true
      package:
        name:
          - openvswitch-switch
          - python3.8-dev
        state: present
